# 安全与隐私

中文 | [English](SECURITY.md)

网关和控制台只监听 `127.0.0.1`。模型和管理接口需要网关密钥，上游地址固定为 Google Vertex AI。请保持本地访问，并将凭据放在仓库之外。环境文件、凭据和日志不属于发布内容。

控制台校验 Host / Origin，拒绝跨站请求，使用限时 HttpOnly / SameSite 会话和 CSRF Token，并限制登录失败次数。首次启动在终端显示设置链接，保存网关密钥后该链接失效。配置接口只回传凭据是否存在，不回传值。浏览器存储中不保存密钥。测试回复仅显示在已登录页面，不写入日志。

GUI 配置（含凭据）以明文保存在 `~/.vertex-streaming-anti-truncation/settings.json`，或 `GATEWAY_STATE_DIR` 指定的目录。请选择私人目录。POSIX 文件/目录请求 0600/0700 权限；Windows 继承目录 ACL。保存前校验，使用进程锁和版本检查，再同步并原子替换文件。旧版本保存或替换端口被占用时保留原服务。能读取当前用户文件的本机软件仍在信任边界内。

请求日志不包含提示词、回复、工具参数或凭据。上游 HTTP 错误只返回固定错误码和状态，不回传原始错误正文。进程内最多保留 200 条事件。流式解析对事件和 JSON 状态设有大小限制，不为记录日志而累积整段回复。非流式 JSON 回复上限为 8 MiB。

每条请求最多发起一次模型生成。客户端取消时，网关会中止上游请求。若正文已经发给客户端，随后出现协议错误，连接会关闭，日志会记录失败。

`npm run verify` 只使用本地模拟数据。`npm run smoke -- --live` 会发送两条收费请求，每条最多 512 个输出 token。独立网关无法读取其他 router 的额度限制或冷却状态。

报告问题时，可以附上版本、状态码、`requestId` 和 `antiTruncation` 元数据。请勿附上 `.env`、服务账号文件、bearer token 或私人对话。
