# Generate your own random gateway key (at least 16 characters).
GATEWAY_API_KEY=
VERTEX_PROJECT_ID=your-project-id
VERTEX_LOCATION=global
PORT=4781
UPSTREAM_TIMEOUT_MS=600000

# Keep this file OUTSIDE the cloned repository. Forward slashes work on Windows.
GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/service-account.json

# Alternative: omit GOOGLE_APPLICATION_CREDENTIALS and supply a short-lived
# Google OAuth access token. Do not configure both authentication methods.
# VERTEX_ACCESS_TOKEN=
